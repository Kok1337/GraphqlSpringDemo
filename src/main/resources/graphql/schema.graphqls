type Query {
    countries: [Country]
    country(code: ID!): Country
    person(id: ID): Person
}

type Country {
    code: ID!
    name: String!
}

type Person {
    id: ID!
    firstName: String!
    lastName: String!
    country: Country!
}